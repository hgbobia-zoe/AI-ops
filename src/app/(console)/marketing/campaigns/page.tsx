// Marketing campaigns blade — plan and track demand-gen campaigns.

import { CampaignsBoard } from "@/components/marketing/CampaignsBoard";

export const dynamic = "force-dynamic";

export default function CampaignsPage(): React.JSX.Element {
  return <CampaignsBoard />;
}
