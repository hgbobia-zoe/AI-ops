// Marketing outreach blade — top-of-funnel prospect tracker; the win is "quote agreed".

import { OutreachBoard } from "@/components/marketing/OutreachBoard";

export const dynamic = "force-dynamic";

export default function OutreachPage(): React.JSX.Element {
  return <OutreachBoard />;
}
