// Post-Event Overview — the management funnel + metrics + "Where are we losing customers?". The real
// product is the structured funnel data behind the workflow.

import { PostEventOverview } from "@/components/postevent/PostEventOverview";

export const dynamic = "force-dynamic";

export default function PostEventOverviewPage(): React.JSX.Element {
  return <PostEventOverview />;
}
