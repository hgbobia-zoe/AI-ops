// Sales OS index — the worklist (action-queue table + detail panel) is rendered by the shell (layout)
// from the ranked queue, so this route renders nothing itself. The full lead board opens in the modal.

export const dynamic = "force-dynamic";

export default function SalesOsIndex(): null {
  return null;
}
