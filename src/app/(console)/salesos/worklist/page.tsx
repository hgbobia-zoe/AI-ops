// Sales OS — Worklist view. The ranked action queue is rendered by the shell (layout) from the queue
// it already built, so this route renders nothing itself. Clicking a row opens the full lead panel.

export const dynamic = "force-dynamic";

export default function SalesOsWorklist(): null {
  return null;
}
