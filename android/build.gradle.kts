// Top-level build file. Version numbers are declared here and the modules apply
// the plugins with `apply false` so the classpath is resolved once for the build.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
